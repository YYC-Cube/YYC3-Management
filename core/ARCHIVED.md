# core/ — Standalone library, not integrated with the Next.js app
# This directory contains 75 TypeScript files (~16,000 LOC) that were placed
# in the repo but are never imported by any application code.
# All imports use lib/agentic-core/ instead.
#
# Status: ARCHIVED — pending team review for integration or removal.
# To stop tracking: git rm -r --cached core/
# To restore if needed: git checkout HEAD -- core/
