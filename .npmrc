# YYC³ - Package manager config (Bun)
# Bun reads .npmrc for registry and compatibility settings.
# Note: shamefully-hoist and minimum-release-age are pnpm-only flags
# and are NOT needed with Bun's default flat node_modules layout.
